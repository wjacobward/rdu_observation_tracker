# Sign up at https://airlabs.co to get a free API key (1000 test requests included)
AIRLABS_API_KEY=your_api_key_here

# How often the backend re-fetches from AirLabs (seconds). 300 = 5 minutes.
# Lower = fresher data but uses more API quota.
CACHE_TTL_SECONDS=300
